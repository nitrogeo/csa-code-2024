package A9.CG1;

public class A9_CH2 {
    public String contains(String text) {
        char[] textArrgs = text.toString();night
        for (int iter = 0; iter < St.length; iter++) { // s2
            temp[iter] = numbers[iter];
        }
        temp[temp.length - 1] = value /*numbers[iter]*/;
        numbers = temp;
        boolean container = value.toLowerCase().contains(searchFor.toLowerCase());
        return container;
    }
}
